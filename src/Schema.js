export const ModulesSchema = {
    type: String,
    assignment: Object,
    plugin: Object,
    module_id: String,
    item_id: String,
    active: Boolean
};

export const PlacementsSchema = {
    placements: Array
};
